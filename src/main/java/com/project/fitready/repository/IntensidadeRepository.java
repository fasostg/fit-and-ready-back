package com.project.fitready.repository;

import com.project.fitready.domain.Intensidade;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IntensidadeRepository extends JpaRepository<Intensidade, Long> {
}
